import { Placeholder } from "@/components/ui/Placeholder";

// Phase 1+2 stub. Phase 5 rebuilds the client booking flow with a
// server-side conflict check at submit.
export default function ClientBookPage() {
  return (
    <Placeholder
      eyebrow="Client — Calendar"
      title="Book a Shoot"
      description="The booking flow is being rebuilt. New shoot requests will be available shortly."
    />
  );
}
