import { Placeholder } from "@/components/ui/Placeholder";

// Phase 1+2 stub. Phase 3 rebuilds Week / Month / Agenda views against
// `fetchEventsInRange` and the new `CalendarEvent` shape from `_lib/types.ts`.
export default function OwnerCalendarPage() {
  return (
    <Placeholder
      eyebrow="Owner — Calendar"
      title="Calendar"
      description="The calendar is being rebuilt. Week, Month, and Agenda views land in Phase 3."
    />
  );
}
